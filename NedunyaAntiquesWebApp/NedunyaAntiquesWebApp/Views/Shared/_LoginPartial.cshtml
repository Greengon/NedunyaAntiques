@if (Request.IsAuthenticated)
{ // TODO: change this so it will fit our project
    using (Html.BeginForm("LogIn", "Customers", FormMethod.Post, new { id = "loginForm", @class = "navbar-left" }))
    {
        <ul class="nav navbar-nav navbar-right text-right">
            <li>
                @Html.ActionLink("Hello " + @Session["UserName"].ToString() + "!", "edit", "Customers", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li>
                @Html.ActionLink("התנתק", "Logout", "Customers")
            </li>
        </ul>
    }
}
else
{
    <p id="gh-ug-flex" class="guest-text">
        שלום אורח!
        @Html.ActionLink("התחבר", "CustomerLog", "Customers", null, null)
        או
        @Html.ActionLink("הרשם", "CustomerForm", "Customers", null, null)
    </p>
}